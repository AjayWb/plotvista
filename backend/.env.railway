# Railway Production Environment Configuration

# Admin Configuration - REQUIRED: Set a secure password
ADMIN_PASSWORD=your-secure-admin-password-here

# Server Configuration
NODE_ENV=production
PORT=4001

# Database Configuration
# Railway will automatically provide DATABASE_URL - no need to set manually

# CORS Configuration - REQUIRED: Set to your Vercel frontend URL
# Example: https://plotvista.vercel.app
FRONTEND_URL=https://your-frontend-domain.vercel.app

# Optional: JWT Secret for additional security
# Generate with: openssl rand -hex 32
JWT_SECRET=your-jwt-secret-here

# Railway-specific optimizations
RAILWAY_STATIC_URL=
RAILWAY_GIT_COMMIT_SHA=
RAILWAY_ENVIRONMENT=production